## US-055: Fix Footer Position When Few Goals
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Fixed the footer positioning issue in the Daily Goals screen where the navigation buttons (Manage Goals, Reports) would float up when there were few or no goals.

### Implementation details:
1. Added explicit `height: 500px` to `html, body` (in addition to existing min/max-height) to ensure the height chain works properly with percentage-based heights.

2. Added `min-height: 100%` to screen container classes (`.view-goals-screen`, etc.) to ensure they fill the available space.

3. Added `margin-top: auto` to `.screen-footer` - this CSS trick pushes the footer to the bottom of the flex container when there's extra space.

4. Changed `.goals-list-container` from `flex: 1` to `flex: 1 1 auto` for more explicit flex behavior.

### Files modified:
- `src/popup/popup.css` (lines 188-195, 359-369, 403-414, 488-502)

### Key learnings:
- In flexbox layouts, `height: 100%` only works when the parent has an explicit height (not just min/max-height)
- `margin-top: auto` on the last flex item pushes it to the bottom of the container
- `flex: 1 1 auto` is more explicit than `flex: 1` (shorthand for `flex: 1 1 0`)
- The Chrome extension popup has fixed dimensions (380px width, 500-600px height)

### Acceptance criteria met:
- Footer buttons stay at bottom regardless of goal count
- Works with 0 goals (empty state)
- Works with 1-2 goals
- Content area scrolls when many goals
- Layout remains consistent across all scenarios

---

## US-056: Compact View Mode for Daily Goals
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Added a compact view mode toggle for the Daily Goals screen. Users can switch between expanded (default) and compact views using an icon button in the header. The preference is saved to settings.

### Implementation details:

1. **Settings Storage** (`src/utils/storage.js`):
   - Added `compactViewEnabled: false` to default settings

2. **Toggle Button in Header** (`src/popup/popup.js`):
   - Added a view toggle button next to the settings button in the View Goals header
   - Created `header-actions` container to hold both buttons
   - Toggle button shows different icons for expanded vs compact view (grid icon vs lines icon)
   - Added `attachCompactViewToggleListener()` function to handle toggle clicks

3. **Goal Card and List Modifications** (`src/popup/popup.js`):
   - `renderGoalsList()` adds `compact-view` class to goals list when compact mode is enabled
   - `renderGoalCard()` adds `compact` class to individual goal cards when compact mode is enabled

4. **Compact View CSS Styles** (`src/popup/popup.css` - ~200 lines added):
   - Header actions container styling
   - View toggle button with hover/active states
   - Compact goals list with reduced gap
   - Compact goal cards with:
     - Reduced padding
     - Smaller type indicators
     - Hidden timeframe badges
     - Inline body layout (progress + controls on same row)
     - Smaller progress bar
     - Hidden progress text (shows only percentage)
     - Compact controls (no border, smaller buttons)
   - Type-specific compact styles for timer, counter, and checkbox controls
   - Smaller completed indicator in compact mode
   - Smooth transitions for gap and padding changes

### Files modified:
- `src/utils/storage.js` (line 256)
- `src/popup/popup.js` (lines 172-202, 314-315, 399-410, 251-252, 3475-3497)
- `src/popup/popup.css` (lines 2418-2621)

### Key design decisions:
- Compact view hides: timeframe badge, timer display, detailed progress text
- Compact view shows: title, type icon, progress bar, percentage, essential controls
- Both views use the same control patterns (play/pause, +/-, checkbox toggle)
- Controls remain fully functional in both views
- Smooth CSS transitions enhance the user experience when switching views

### Acceptance criteria met:
- Toggle button to switch views (expand/collapse icon)
- Compact view shows: title, type icon, progress bar, essential controls
- Expanded view remains as current (default)
- View preference saved to settings
- Smooth transition between views
- Both views remain functional for all goal types

---

## US-057: Dynamic Goal Form Fields Based on Type
**Date**: 2026-01-22
**Status**: Completed (Already Implemented)

### What was done:
Verified that the dynamic goal form fields feature was already implemented in the codebase. The Add/Edit goal form shows only relevant fields based on the selected goal type.

### Implementation details (pre-existing):

1. **HTML Structure** (`src/popup/popup.html`):
   - `#timer-target-group` (lines 118-153): Contains hours/minutes inputs for timer goals
   - `#counter-target-group` (lines 156-170): Contains target count input for counter goals
   - Both groups have the `target-input-group` class for shared styling
   - Counter group starts with `hidden` class (timer is default type)

2. **JavaScript Logic** (`src/popup/popup.js`):
   - `updateTargetInputVisibility(type)` (lines 1914-1937): Toggles visibility based on type
   - `setFormType(type)` (lines 1871-1897): Sets form type and calls updateTargetInputVisibility
   - `handleTypeSelection(type)` (lines 1903-1906): Handles type button clicks
   - `handleGoalFormSubmit()` (lines 1605-1724): Validates only relevant fields based on type
   - `openGoalModal('edit', goal)` (lines 1458-1521): Pre-fills form and shows correct fields in edit mode

3. **CSS Styles** (`src/popup/popup.css`):
   - `.target-input-group` (lines 3108-3111): Base styles with smooth transitions
   - `.target-input-group.hidden` (lines 3114-3119): Hidden state with opacity: 0, max-height: 0
   - Transition: `opacity 200ms ease, max-height 200ms ease, margin 200ms ease`

### How it works:
- **Timer type**: Shows `#timer-target-group` (hours/minutes inputs), hides `#counter-target-group`
- **Counter type**: Shows `#counter-target-group` (target count input), hides `#timer-target-group`
- **Checkbox type**: Hides both target groups (target is always 1)

### Files involved:
- `src/popup/popup.html` (lines 118-170)
- `src/popup/popup.js` (lines 1605-1937)
- `src/popup/popup.css` (lines 3108-3119)

### Key learnings:
- The feature was implemented as part of earlier user stories (US-025, US-026)
- Good practice to check if functionality already exists before implementing
- The hidden class approach with CSS transitions provides smooth animations

### Acceptance criteria met:
- Timer selected: show hours/minutes inputs only
- Counter selected: show target count input only
- Checkbox selected: hide all target inputs
- Fields animate in/out smoothly (200ms CSS transitions)
- Form validates only visible fields (conditional validation in handleGoalFormSubmit)
- Edit mode pre-selects type and shows correct fields (setFormType called in openGoalModal)

---

## US-058: Full-Page Add/Edit Goal Screen
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Replaced the modal/popup for Add/Edit goal with a dedicated full-page screen. This provides better UX with more space for form fields and maintains consistency with the app's navigation pattern.

### Implementation details:

1. **Screen Registration** (`src/popup/popup.js`):
   - Added `GOAL_FORM: 'goalForm'` to SCREENS enum
   - Added `[SCREENS.GOAL_FORM]: 'screen-goal-form'` to SCREEN_IDS mapping
   - Added case in `renderCurrentScreen()` switch to call `renderGoalFormScreen()`

2. **HTML Structure** (`src/popup/popup.html`):
   - Added new screen container: `<div id="screen-goal-form" class="screen">`
   - Removed the old goal form modal HTML (US-022 modal code)

3. **New Screen Rendering** (`src/popup/popup.js` - ~600 lines added):
   - `goalFormState` object to track mode ('add'/'edit') and editingGoalId
   - `openGoalFormScreen(mode, goal)` - Sets state and navigates to form screen
   - `renderGoalFormScreen()` - Renders full-page form with:
     - Header with back button and dynamic title ("Add New Goal" / "Edit Goal")
     - Main content area with all form fields (title, type selector, targets, timeframe)
     - Footer with Cancel and Save buttons
   - `attachGoalFormScreenListeners()` - Event listeners for all form interactions
   - `setGoalFormScreenType()` - Updates type selection and shows/hides target inputs
   - `setGoalFormScreenTimeframe()` - Updates timeframe selection and hint text
   - `prefillGoalFormScreen()` - Pre-fills form data for edit mode
   - `handleGoalFormScreenSubmit()` - Validates and saves goal (add or edit)

4. **Navigation Updates** (`src/popup/popup.js`):
   - Add Goal button now calls `openGoalFormScreen('add')` instead of `openGoalModal('add')`
   - Edit button (handleEditGoal) now calls `openGoalFormScreen('edit', goal)` instead of `openGoalModal('edit', goal)`
   - Back and Cancel buttons navigate to SCREENS.MANAGE_GOALS

5. **CSS Styles** (`src/popup/popup.css` - ~80 lines added):
   - `.goal-form-screen` - Full-height flex container
   - `.goal-form-header` - Consistent header with back button, title, spacer
   - `.goal-form-content` - Scrollable content area
   - `.goal-form-footer` - Fixed footer with action buttons

### Files modified:
- `src/popup/popup.html` (removed modal, added screen placeholder)
- `src/popup/popup.js` (lines 78-95, 143-162, 1133-1140, 1180-1765, 1821-1824, 4474-4484)
- `src/popup/popup.css` (lines 1309-1388)

### Key design decisions:
- Form fields are dynamically rendered in JavaScript (not static HTML) for consistency
- Reused existing validation helpers (showInputError, clearInputError)
- Form state is managed via dedicated `goalFormState` object, not DOM attributes
- Old modal functions retained (but unused) for backward compatibility with tests

### Architecture notes:
- Full-page screens follow consistent pattern: header (back, title, spacer) + main + footer
- Navigation is unidirectional: Manage Goals → Goal Form → Manage Goals
- Form submission navigates back automatically on success
- Error handling shows inline errors without blocking navigation

### Acceptance criteria met:
- New SCREENS.GOAL_FORM screen added
- Full-page layout with header (back button, title)
- All form fields from current modal (title, type, targets, timeframe)
- Save/Cancel buttons at bottom
- Back navigation returns to Manage Goals
- Old modal code removed from HTML

---

## US-059: Drag-to-Reorder Daily Goals
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Implemented drag-to-reorder functionality for goals in the Daily Goals screen using the HTML5 Drag and Drop API. Users can now drag goals by their grip handle to reorder them, with visual feedback during the drag operation.

### Implementation details:

1. **Drag Handle Added to Goal Cards** (`src/popup/popup.js`):
   - Added `draggable="true"` attribute to goal cards
   - Added a grip icon (6-dot pattern) as a drag handle before the title row
   - Handle is visible on all goal cards and styled for both expanded and compact views

2. **Drag and Drop Event Handlers** (`src/popup/popup.js` lines 1043-1280):
   - `attachDragDropListeners()` - Attaches all drag/drop event listeners to goal cards
   - `handleDragStart()` - Initiates drag, stores dragged goal ID, adds visual feedback
   - `handleDragEnd()` - Cleans up drag state and removes visual classes
   - `handleDragOver()` - Determines if dropping above or below target card
   - `handleDragEnter()` - Adds drag-over visual feedback
   - `handleDragLeave()` - Removes drag-over feedback when leaving card
   - `handleDrop()` - Processes the drop and triggers reorder
   - `handleGoalsListDragOver/Drop()` - Handles drops at the end of the list

3. **Reorder Logic** (`src/popup/popup.js` lines 1244-1280):
   - `reorderGoals(draggedId, targetId, dropAbove)` - Core reordering function
   - Removes dragged goal from array, inserts at new position
   - Updates `order` property for all goals (sequential index)
   - Persists to storage via `saveGoals()`
   - Re-renders the screen to reflect new order

4. **CSS Styles** (`src/popup/popup.css` lines 2704-2813):
   - `.drag-handle` - Grip icon styling with hover effects
   - `.goal-card.dragging` - Semi-transparent, scaled-down state
   - `.drag-over-top/.drag-over-bottom` - Drop position indicators (colored bars)
   - `.drag-placeholder` - Dashed border placeholder element
   - Compact mode adjustments for smaller drag handle

### Key design decisions:
- Drag handle positioned at the start of the card header for easy access
- Visual feedback uses border indicators above/below target cards
- The entire card is draggable, but the handle provides a clear affordance
- Order is persisted immediately on drop (no undo for now)
- Works seamlessly in both expanded and compact view modes

### Files modified:
- `src/popup/popup.js` (lines 421-432, 1039-1280)
- `src/popup/popup.css` (lines 2704-2813)

### Key learnings:
- HTML5 DnD requires careful handling of dragover/dragleave for child elements
- Using `closest()` helps handle events bubbling from child elements
- Updating order property for all goals ensures consistent sorting
- The existing `order` property in the Goal model was already designed for this feature

### Acceptance criteria met:
- Drag handle visible on goal cards (6-dot grip icon)
- Drag and drop works smoothly (HTML5 DnD API)
- Visual feedback (ghost, placeholder) during drag (opacity, drop indicators)
- Order persisted to storage (saveGoals called after reorder)
- Order maintained after refresh (goals sorted by order property)
- Works in both expanded and compact views (responsive CSS styles)

---

## US-060: UI Design Document
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Created a comprehensive UI design document (plans/DESIGN.md) that defines consistent patterns for spacing, typography, colors, buttons, forms, cards, icons, and animations. Also documented current inconsistencies that should be fixed.

### Implementation details:

1. **Document Structure**: Created 14 comprehensive sections covering all UI aspects:
   - Design Principles
   - Spacing System (4px/8px base)
   - Typography Scale
   - Color Palette (light and dark modes)
   - Shadows & Elevation
   - Border Radius
   - Transitions & Animations
   - Button Variants
   - Form Patterns
   - Card Components
   - Icon Usage
   - Animation Standards
   - Current Inconsistencies
   - Accessibility Guidelines

2. **Spacing System**: Documented the 4px base unit system:
   - `--spacing-xs`: 4px (icon gaps, tight spacing)
   - `--spacing-sm`: 8px (component internal padding)
   - `--spacing-md`: 16px (card padding, standard gaps)
   - `--spacing-lg`: 24px (section margins)
   - `--spacing-xl`: 32px (page margins)

3. **Typography Scale**: Documented 6 font sizes with usage:
   - `--font-size-xs` (11px): Badges, timestamps
   - `--font-size-sm` (12px): Labels, captions
   - `--font-size-base` (14px): Body, buttons, inputs
   - `--font-size-lg` (16px): Card titles
   - `--font-size-xl` (18px): Screen titles
   - `--font-size-2xl` (24px): Large numbers

4. **Color System**: Documented all CSS variables for:
   - Primary colors (green) for actions/success
   - Secondary colors (blue) for timers/info
   - Status colors (success, danger, warning)
   - Neutral colors (backgrounds, text, borders)
   - Special colors (counter orange, yearly purple, trophy gold)
   - Goal type specific colors
   - Timeframe badge colors
   - Dark mode adjustments

5. **Button Variants**: Documented 4 button types:
   - Primary (green, main actions)
   - Secondary (outlined, cancel actions)
   - Danger (red, destructive actions)
   - Ghost (transparent, subtle actions)
   - Icon buttons (various sizes)

6. **Form Patterns**: Documented structure and states:
   - Form group layout
   - Input styles and states (focus, error, valid)
   - Type selector button groups
   - Error message display

7. **Card Patterns**: Documented goal card structure:
   - Header, body, controls sections
   - Compact variant modifications
   - Management list items

8. **Animation Standards**: Documented keyframe animations:
   - Button press (scale 0.95)
   - Checkbox toggle (pop effect)
   - Completion badge appearance
   - Timer pulse (active state)
   - Progress shimmer (completed)
   - Flame glow (streak)
   - Empty state float

9. **Current Inconsistencies Identified**:
   - Spacing: Inconsistent gaps, button padding variance
   - Typography: Hardcoded font sizes, letter-spacing variance
   - Colors: Counter/yearly/gold colors not in CSS variables
   - Components: Duplicated back button styles, hardcoded spacer width
   - Borders: Inconsistent border widths
   - Accessibility: Missing focus states, contrast issues

### Files created:
- `plans/DESIGN.md` (~500 lines)

### Key learnings:
- The codebase already uses CSS variables extensively, which is excellent
- The 4px/8px spacing system is well-established
- Some colors (counter orange, yearly purple) need to be added as CSS variables
- Back button styles are duplicated across headers - candidate for refactoring
- Animation patterns are consistent and well-structured

### Acceptance criteria met:
- Spacing system (4px/8px base unit)
- Typography scale (headings, body, labels)
- Color palette usage guide
- Button variants and states
- Form field patterns
- Card component patterns
- Icon usage guidelines
- Animation/transition standards
- List of current inconsistencies

---

## US-061: Refactor UI to Match Design Document
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Applied the design document standards consistently across all screens by adding missing CSS variables, replacing hardcoded values, consolidating duplicated styles, and adding accessibility improvements.

### Implementation details:

1. **Added Missing CSS Variables** (Light and Dark modes):
   - `--counter-color`: #E65100 (counter goal accent color)
   - `--counter-accent`: #FB8C00 (counter button/border accent)
   - `--counter-light`: #FFF3E0 (counter background)
   - `--yearly-color`: #7B1FA2 (yearly timeframe purple)
   - `--yearly-light`: #F3E5F5 (yearly background)
   - `--gold`: #FFB300 (best streak/trophy)
   - `--gold-bright`: #FFD700 (sparkles/confetti)

2. **Replaced Hardcoded Colors with CSS Variables**:
   - All `#E65100` usages → `var(--counter-color)`
   - All `#FB8C00` usages → `var(--counter-accent)`
   - All `#F57C00` usages → `var(--counter-color)`
   - All `#7B1FA2` usages → `var(--yearly-color)`
   - All `#F3E5F5` usages → `var(--yearly-light)`
   - All `#FFB300` usages → `var(--gold)`
   - All `#FFD700` usages → `var(--gold-bright)`

3. **Consolidated Duplicated Back Button Styles**:
   - Enhanced base `.back-btn` style with hover, active, and focus-visible states
   - Removed duplicate styles from `.manage-goals-header .back-btn`
   - Removed duplicate styles from `.goal-form-header .back-btn`
   - Removed duplicate styles from `.reports-header .back-btn`
   - Each header now only has minimal overrides (padding, font-weight)

4. **Fixed Header Spacer**:
   - Changed from hardcoded `width: 70px` to flexible `flex: 0 0 auto; min-width: 70px`

5. **Added Accessibility - Reduced Motion Support**:
   - Added `@media (prefers-reduced-motion: reduce)` query
   - Disables animations, transitions, and scroll behavior for users who prefer reduced motion
   - Respects system accessibility settings

### Files modified:
- `src/popup/popup.css` (multiple sections, ~100 lines changed/consolidated)

### Locations of key changes:
- CSS variables: lines 52-59 (light mode), lines 149-156 (dark mode)
- Reduced motion: lines 190-199
- Base back button: lines 406-430
- Manage goals header: lines 974-987
- Goal form header: lines 1347-1360
- Reports header: lines 3889-3898

### Key design decisions:
- Kept small gap values (1-4px) as-is since they're intentional for tight spacing
- Used `--counter-light` instead of `--warning-light` for counter-specific backgrounds for semantic clarity
- Back button base style now includes border-radius and focus-visible for better accessibility

### Acceptance criteria met:
- All spacing uses design system values (CSS variables throughout)
- Buttons consistent across screens (consolidated back button styles)
- Typography matches design doc (already using font variables)
- Colors used consistently (all hardcoded colors now use CSS variables)
- Cards have uniform styling (already using design system)
- Forms follow standard patterns (already established)
- No visual regressions (CSS changes maintain same visual appearance)

---

## US-062: Fix Stats Group Alignment in Daily Goals
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Fixed the alignment issues in the Daily Goals screen header stats group (completed count and streak). The stats items now have proper vertical centering with icons and text properly aligned.

### Implementation details:

1. **Header Stats Container** (`.header-stats`):
   - Added `min-height: 40px` to ensure consistent container height

2. **Stat Item Alignment** (`.stat-item`):
   - Added `line-height: 1` to prevent inconsistent text baseline issues

3. **Stat Icon Alignment** (`.stat-icon`):
   - Changed from simple font-size styling to a fixed-size container approach
   - Added `display: inline-flex` with `align-items: center` and `justify-content: center`
   - Set fixed dimensions: `width: 20px; height: 20px`
   - Added `flex-shrink: 0` to prevent icon shrinking

4. **Stat Value and Label**:
   - Added `line-height: 1` to both `.stat-value` and `.stat-label` for consistent text alignment

5. **Stat Divider** (`.stat-divider`):
   - Reduced height from 24px to 20px to match icon container height
   - Added `align-self: center` for explicit vertical centering
   - Added `flex-shrink: 0` to prevent divider shrinking

### Files modified:
- `src/popup/popup.css` (lines 631-713)

### Key design decisions:
- Used a fixed-size container (20x20px) for icons to ensure consistent alignment regardless of icon content (emoji vs text character)
- The flame emoji (🔥) and checkmark character now occupy the same visual space
- Setting `line-height: 1` on flex items removes default line-height spacing that causes misalignment
- The divider height was reduced to match the visual height of the icon containers

### Acceptance criteria met:
- Stats items vertically centered (flexbox with align-items: center)
- Consistent spacing between stats (gap: var(--spacing-md) maintained)
- Icons aligned with text (fixed-size icon containers with inline-flex centering)
- Responsive on different content lengths (flex-shrink: 0 prevents shrinking)
- Matches overall design system (uses CSS variables, follows spacing patterns)

---

## US-063: Fix Completion Checkmark Overlay Position
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Fixed the completion checkmark overlay position so it no longer obscures the goal card content. The checkmark was repositioned from the top-right corner to the bottom-right corner of the goal card.

### Implementation details:

1. **Main Completed Indicator** (`src/popup/popup.css` lines 2323-2339):
   - Changed `top: var(--spacing-sm)` to `bottom: var(--spacing-sm)`
   - Added `z-index: 2` to ensure it stays above other card elements
   - Added comment documenting the US-063 change

2. **Compact View Indicator** (`src/popup/popup.css` lines 2703-2710):
   - Changed `top: var(--spacing-xs)` to `bottom: var(--spacing-xs)`
   - Updated comment to document the change

### Files modified:
- `src/popup/popup.css` (lines 2323-2339, 2703-2710)

### Key design decisions:
- Bottom-right positioning was chosen because:
  - The top-right area contains the timeframe badge which was being covered
  - The title and type indicator are in the top-left area
  - Controls are centered in the card body
  - Bottom-right provides a clear, unobstructed location
- The checkmark maintains its green success color and circular badge design
- Animation remains intact (completion-badge-appear keyframes)
- Both expanded and compact views use consistent bottom-right positioning

### Acceptance criteria met:
- Checkmark doesn't cover goal title (moved to bottom corner)
- Checkmark doesn't cover progress info (positioned in bottom-right, away from progress bar)
- Checkmark doesn't cover controls (controls are centered, checkmark is in corner)
- Clear visual indication of completion (green badge with white checkmark, same visibility)
- Works for all goal types (CSS applies to all .goal-completed-indicator elements)
- Works in both view modes (updated both expanded and compact view styles)

---

## US-064: Enhanced Completion Confetti
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Implemented an enhanced confetti animation system for goal completion celebrations. The new system uses both a canvas-based JavaScript confetti and improved CSS pseudo-element animations for a more impressive and satisfying celebration effect.

### Implementation details:

1. **Canvas-Based Confetti System** (`src/popup/popup.js` lines 514-740):
   - Created `ConfettiParticle` class with physics-based animation (gravity, drag, wobble)
   - Three particle shapes: rectangles, circles, and strips for visual variety
   - Rotation animation for realistic falling effect
   - Opacity fade-out near bottom of screen
   - `getConfettiColors()` function reads CSS variables to match app theme
   - `launchConfetti()` function creates canvas overlay and manages animation loop
   - Configurable intensity levels: 'low' (30 particles), 'normal' (50), 'high' (80)
   - Respects `prefers-reduced-motion` accessibility setting

2. **Enhanced CSS Confetti** (`src/popup/popup.css` lines 2398-2488):
   - Primary burst (::before) with 6 colors, larger particles (3-4px)
   - Secondary burst (::after) with delayed animation for layered effect
   - Rotation added to burst animations for more dynamic feel
   - Longer duration (1.2s primary, 1.4s secondary) vs old 0.8s
   - Cubic-bezier easing for smoother animation curves
   - Expanded scale (2.5x and 3x) for more impressive spread

3. **Updated Celebration Function** (`src/popup/popup.js` lines 742-772):
   - `triggerCompletionCelebration()` now accepts optional `intensity` parameter
   - Launches canvas confetti alongside CSS animation
   - Extended timeout to 2000ms to match new animation duration

### Files modified:
- `src/popup/popup.js` (lines 509-772, ~260 lines added)
- `src/popup/popup.css` (lines 2398-2488, enhanced from ~37 lines to ~91 lines)

### Key design decisions:
- Used canvas for true particle physics (gravity, drag, wobble) that CSS can't achieve
- Kept CSS pseudo-element animation as a complement for instant visual feedback
- Colors pulled from CSS variables ensure theme consistency (light/dark mode)
- Three intensity levels allow for future differentiation (e.g., streak milestones)
- Animation duration of 2 seconds is celebratory but not disruptive
- Respects prefers-reduced-motion for accessibility

### Color palette used:
- `--success` (green) - primary completion color
- `--gold-bright` (gold/yellow) - celebratory highlight
- `--secondary` (blue) - variety
- `--warning` (orange) - variety
- `#FF6B6B` (coral red) - added for more color variety
- `#A78BFA` (purple) - added for more color variety

### Acceptance criteria met:
- Confetti is visually impressive (50 canvas particles + CSS burst animation)
- Colors match app theme (reads from CSS variables, works in dark mode)
- Animation is smooth (60fps via requestAnimationFrame)
- Duration is satisfying but not too long (2 seconds)
- Works for all goal completions (timer, counter, checkbox)
- Different intensity for different achievements (low/normal/high parameter available)

---

## US-065: Goal Categories with Color Coding
**Date**: 2026-01-22
**Status**: Completed

### What was done:
Implemented a comprehensive goal categories feature with color coding. Users can assign categories to goals, filter goals by category, and manage categories in Settings.

### Implementation details:

1. **Storage Layer** (`src/utils/storage.js`):
   - Added `CATEGORIES` storage key
   - Defined `DEFAULT_CATEGORIES` with 4 predefined categories:
     - Work (blue: #2196F3)
     - Health (green: #4CAF50)
     - Learning (purple: #9C27B0)
     - Personal (orange: #FF9800)
   - Added functions: `getCategories()`, `saveCategories()`, `getDefaultCategories()`, `addCategory()`, `deleteCategory()`, `getCategoryById()`

2. **State Management** (`src/popup/popup.js`):
   - Added `categories` array to global state
   - Added `categoryFilter` to track current filter ('all' or category id)
   - Categories loaded during `loadData()` alongside other data

3. **Goal Form Screen** (`src/popup/popup.js`):
   - Added category selector with button group pattern (matching type/timeframe selectors)
   - "None" option for goals without category
   - Pre-fills category when editing existing goals
   - Category saved with goal on submit

4. **Goal Card Display** (`src/popup/popup.js`):
   - Added `data-category` attribute to goal cards for CSS styling
   - Category badge displayed alongside timeframe badge with color dot
   - Category color stripe on left side of card (via CSS ::before)

5. **Category Filter Bar** (`src/popup/popup.js`):
   - Filter bar displayed in View Goals header when goals have categories
   - "All" chip plus one chip per category with color dot
   - Clicking chip filters goals list to that category
   - Empty state message when no goals match filter

6. **Settings Screen** (`src/popup/popup.js`):
   - New "Categories" section between Appearance and About
   - List of all categories with color indicator
   - Default categories marked with "Default" badge
   - Delete button for custom categories (with confirmation if goals use it)
   - "Add Custom Category" button with prompt dialog
   - Automatic color selection from preset palette for new categories

7. **CSS Styles** (`src/popup/popup.css`):
   - Category badge styles (similar to timeframe badges)
   - Category color dot indicator
   - Goal card side stripe via ::before pseudo-element
   - Category selector styles (button group pattern)
   - Category filter bar with chips
   - Category management list in settings
   - Dark mode support for all category styles

### Files modified:
- `src/utils/storage.js` (lines 7-28, 314-396, 398-427)
- `src/popup/popup.js` (lines 10-28, 63-74, 229, 267-268, 281-307, 349-382, 405-452, 1909-1925, 2001-2037, 2113-2132, 2174-2176, 2258-2264, 2268-2308, 4559-4589, 4680-4681, 4709-4806, 4727-4733)
- `src/popup/popup.css` (lines 942-1007, 789-802, 1642-1712, 3781-4040)

### Key design decisions:
- Used button group selector pattern for categories (consistent with type/timeframe)
- Category side stripe provides clear visual indication without cluttering UI
- Filter bar only appears when goals have categories assigned
- Default categories cannot be deleted, only custom ones
- Deleting a category used by goals prompts confirmation and clears category from those goals
- Custom categories use a preset color palette with automatic selection
- Category is optional - "None" option available

### Acceptance criteria met:
- Predefined categories with colors (work=blue, health=green, learning=purple, personal=orange)
- Custom category creation with color picker (uses preset palette with auto-selection)
- Category selector in goal form (button group with color indicators)
- Visual category indicator on goal cards (color badge + side stripe)
- Filter goals by category in View Goals (filter bar with chips)
- Category management in Settings screen (list, add, delete)

